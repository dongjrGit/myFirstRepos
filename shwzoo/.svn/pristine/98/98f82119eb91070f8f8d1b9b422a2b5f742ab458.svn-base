package com.yinlian.Enums;

/**
 * 操作记录类型
 * 
 * @author mjx
 *
 */
public enum OperateRecordsTypeEnum {
	查看(0), 修改(1), 添加(2), 删除(3), 登录(4), 查询(5), 导入(6), 导出(7), 发送邮件(80), 发送短信(9), 发送站内信(10);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	OperateRecordsTypeEnum(int value) {
		this.value = value;
	}
}
