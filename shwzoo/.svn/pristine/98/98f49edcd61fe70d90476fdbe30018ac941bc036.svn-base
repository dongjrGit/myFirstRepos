package com.yinlian.Enums;

/**
 * 排序方式
 * 
 * @author mjx
 *
 */
public enum OrderByTypeEnum {
	降序(0), 升序(1);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	OrderByTypeEnum(int value) {
		this.value = value;
	}
}
