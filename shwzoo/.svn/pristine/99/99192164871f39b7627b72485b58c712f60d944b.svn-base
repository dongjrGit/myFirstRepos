package com.yinlian.Enums;

/**
 * @author mjx 保证金操作
 */
public enum FinanceTypeEnum {
	无(-1), 支出(0), 收入(1), 充值(2), 礼品卡充值(3);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	FinanceTypeEnum(int value) {
		this.value = value;
	}
}
