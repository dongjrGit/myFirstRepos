package com.yinlian.Enums;

/**
 * 经彩豆兑换
 * 
 * @author mjx
 *
 */
public enum ExchangeType {
	增加经彩豆(1), 减少经彩豆(0);
	private Integer val;

	public Integer getValue() {
		return val;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	ExchangeType(Integer value) {
		val = value;
	}
}
