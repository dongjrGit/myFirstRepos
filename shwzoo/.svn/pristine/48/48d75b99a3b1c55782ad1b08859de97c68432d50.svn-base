package com.yinlian.Enums;

/**
 * 积分记录来源类型
 * 
 * @author mjx
 *
 */
public enum PointsRecordsFromTypeEnum {
	支付订单(0), 订单评价(1), 签到(2), 活动增送(3), 订单消费(4), 活动消费(5);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	PointsRecordsFromTypeEnum(int value) {
		this.value = value;
	}
}
