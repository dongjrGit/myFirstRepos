package com.yinlian.Enums;

/**
 * 组合商品类型
 * 
 * @author mjx
 *
 */
public enum PackageTypeEnum {
	限时抢购(0), 组合商品(1);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	PackageTypeEnum(int value) {
		this.value = value;
	}
}
