package com.yinlian.Enums;

/**
 * 广告类型
 * 
 * @author mjx
 *
 */
public enum AdvertisingType {
	外部链接(0), 店铺(1), 商品(2), 专题(3);
	
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	AdvertisingType(int value) {
		this.value = value;
	}
}
