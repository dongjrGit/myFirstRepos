package com.yinlian.Enums;

/**
 * 活动记录子表 状态
 * 
 * @author mjx
 *
 */
public enum Och_Status {
	未增送(0), 已增送(1);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	Och_Status(int value) {
		this.value = value;
	}
}