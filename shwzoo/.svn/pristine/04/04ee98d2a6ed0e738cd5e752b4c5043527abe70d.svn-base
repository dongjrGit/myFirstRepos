package com.yinlian.Enums;

/**
 * 优惠劵来源
 * @author Administrator
 *
 */
public enum CouponFromTypeEnum {
	平台(0), 外部(1);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	CouponFromTypeEnum(int value) {
		this.value = value;
	}
}
