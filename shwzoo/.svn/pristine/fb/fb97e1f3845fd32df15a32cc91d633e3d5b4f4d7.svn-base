package com.yinlian.Enums;

/** 支付密码状态 */
public enum PayPwdStatusEnum {
	未设置(0), 开启(1), 禁用(2);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	PayPwdStatusEnum(int value) {
		this.value = value;
	}
}
