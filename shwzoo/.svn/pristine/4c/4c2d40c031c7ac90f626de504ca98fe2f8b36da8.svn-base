package com.yinlian.Enums;

public enum SearchAttrType_Enum {
	商品属性(0), 商品规格(1), 商品品牌(2),商品价格(3);
	private final Integer value;

	public Integer getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	SearchAttrType_Enum(Integer value) {
		this.value = value;
	}
}
