package com.yinlian.Enums;

/**
 * 商品列表排序字段
 * 
 * @author mjx
 *
 */
public enum ProOrderByEnum {
	销量(0), 价格(1), 评论数(2);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	ProOrderByEnum(int value) {
		this.value = value;
	}
}
