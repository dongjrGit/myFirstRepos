package com.yinlian.Enums;

/**
 * 抢购类型
 * 
 * @author mjx
 *
 */
public enum SpikeTypeEnum {
	秒杀(0), 闪购(1),促销(2),经彩活动(3),团购(4);
	private final int value;

	public int getValue() {
		return value;
	}

	// 构造器默认也只能是private, 从而保证构造函数只能在内部使用
	SpikeTypeEnum(int value) {
		this.value = value;
	}
}
